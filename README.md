# Numpy Skip-Gram implementation

- model.py includes the model definition and the dataset handling, while the train.ipynb includes the training.
- the embeddings folder contains the embedding matrix and the word to index mapping dictionary.